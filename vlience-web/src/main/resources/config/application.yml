spring:
  main:
    lazy-initialization: true
    banner-mode: off
  aop:
    auto: true
    proxy-target-class: true
  application:
    name: org.iskyc.lulech.main.Application
  beaninfo:
    ignore: true
  config:
    name: application
  elasticsearch:
    uris: http://localhost:9200
server:
  port: 8989
